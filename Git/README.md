# Git notes
## Git Commands
- Check repo status:
```bash
#Stage all files/folders in active directory
git statu 
```
- Stage files or list of files `git add <PATH>`:
```bash
#Stage all files/folders in active directory
git add . 
#Stage specific file
git add file.txt
```
